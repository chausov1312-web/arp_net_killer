# ARP Internet Killer Tool 🔥

Мощный инструмент для тестирования безопасности сетей с помощью ARP-spoofing атак. Позволяет проводить агрессивное сканирование сети и выполнять атаки типа "человек посередине" (Man-in-the-Middle) на несколько целей одновременно.

## ⚠️ ВНИМАНИЕ

**Этот инструмент предназначен только для:**
- Тестирования собственных сетей
- Образовательных целей
- Аудита безопасности сетей с письменного разрешения

**Использование этого инструмента для атак на сети без разрешения является незаконным!**

## ✨ Особенности

- **🔍 Агрессивное сканирование сети** - комбинированные методы обнаружения устройств
- **🎯 Множественный выбор целей** - атака сразу на несколько устройств
- **🌐 Автоопределение шлюза** - интеллектуальное определение роутера/шлюза
- **📡 Поддержка Termux** - работает на Android устройствах через Termux
- **🎨 Интуитивный интерфейс** - цветной вывод и меню через fzf
- **🔄 Автовосстановление** - автоматическое восстановление ARP таблиц при остановке
- **⚡ Быстрая атака** - эффективная отправка ARP пакетов

## 📦 Установка

### Предварительные требования

```bash
# Для Linux/Debian/Ubuntu
sudo apt update
sudo apt install -y python3 python3-pip nmap fzf

# Для Termux (Android)
pkg update
pkg install -y python nmap fzf root-repo
pkg install -y tsu  # для root прав
```

### Установка зависимостей

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/arp-killer.git
cd arp-killer

# Установка Python зависимостей
pip3 install -r requirements.txt

# Или установка вручную
pip3 install scapy
```

### Настройка прав

```bash
# Дать права на выполнение
chmod +x main.py

# Или установить как системную команду
sudo cp main.py /usr/local/bin/arp-killer
```

## 🚀 Использование

### Базовый запуск

```bash
# Запуск с правами root
sudo python3 main.py

# Если установлен как системная команда
sudo arp-killer
```

### Для Termux (Android)

```bash
# Запуск с правами root в Termux
tsu
python main.py
```

## 📖 Руководство по использованию

### 1. Выбор интерфейса
Программа автоматически определит доступные сетевые интерфейсы:
- `wlan0` - Wi-Fi адаптер
- `eth0` - Ethernet адаптер
- `usb0` - USB адаптер

### 2. Автоматическое определение шлюза
Инструмент использует несколько методов для определения шлюза:
- Анализ таблицы маршрутизации
- Проверка DNS серверов
- ARP сканирование
- Для Termux: анализ системных свойств Android

### 3. Режимы работы

#### 🔍 Режим сканирования
1. Автоматическое сканирование сети
2. Просмотр всех обнаруженных устройств
3. Выбор целей из списка
4. Запуск атаки

#### 📝 Ручной режим
1. Ввод IP шлюза вручную
2. Ввод IP целей вручную
3. Автоматическое определение MAC адресов

### 4. Управление атакой

**Во время атаки:**
- Отображается количество отправленных пакетов
- Показывается время работы
- Список атакуемых устройств

**Для остановки:**
```
Нажмите Ctrl+C
```
Программа автоматически восстановит ARP таблицы всех жертв.

## 🛠️ Архитектура проекта

```
arp_killer/
├── main.py                 # Основной файл программы
├── attack.py              # Класс ARPAttack для управления атаками
├── modules/               # Модули программы
│   ├── __init__.py
│   ├── banner.py          # Отображение баннера
│   ├── fzf_selector.py    # Меню выбора через fzf
│   ├── interface_manager.py # Управление сетевыми интерфейсами
│   ├── arp_utils.py       # ARP функции (определение MAC и т.д.)
│   ├── gateway_detector.py # Определение шлюза
│   └── network_scanner.py # Сканирование сети
└── requirements.txt       # Зависимости Python
```

## 🔧 Технические детали

### Методы сканирования
1. **Nmap** - быстрый ping sweep сканирование
2. **ARP запросы** - агрессивное ARP сканирование
3. **Анализ ARP таблиц** - чтение существующих записей

### Определение шлюза
Программа использует до 5 методов определения шлюза:
1. `ip route` - анализ таблицы маршрутизации
2. `route -n` - классическая команда route
3. `netstat -rn` - сетевые соединения
4. `/etc/resolv.conf` - анализ DNS серверов
5. Для Android: `getprop` - системные свойства

### Особенности работы с Termux
- Использование `ipconfig` для Android
- Анализ системных свойств через `getprop`
- Поддержка Android Wi-Fi интерфейсов

## ⚙️ Настройка

### Конфигурация через переменные окружения

```bash
# Установка интерфейса по умолчанию
export ARP_INTERFACE=wlan0

# Настройка скорости атаки (пакетов в секунду)
export ARP_SPEED=5

# Включение расширенного логирования
export ARP_DEBUG=true
```

## 🐛 Решение проблем

### Проблема: "Требуются права root!"
```bash
# Решение для Linux
sudo python3 main.py

# Решение для Termux
tsu
python main.py
```

### Проблема: "fzf не найден"
```bash
# Установка fzf
sudo apt install fzf

# Для Termux
pkg install fzf
```

### Проблема: "Nmap не найден"
```bash
# Установка nmap
sudo apt install nmap

# Для Termux
pkg install nmap
```

### Проблема: "Scapy не установлен"
```bash
pip3 install scapy
# или
pip install --user scapy
```

## 📊 Примеры использования

### Пример 1: Быстрая атака на одного пользователя
```bash
sudo python3 main.py
# 1. Выберите интерфейс wlan0
# 2. Выберите режим "Сканирование"
# 3. Выберите жертву из списка
# 4. Подтвердите атаку
```

### Пример 2: Атака на несколько устройств
```bash
sudo python3 main.py
# 1. Выберите интерфейс
# 2. Режим "Сканирование"
# 3. Выберите несколько жертв (через пробел)
# 4. Запустите атаку
```

### Пример 3: Ручной ввод целей
```bash
sudo python3 main.py
# 1. Выберите интерфейс
# 2. Режим "Ручной ввод"
# 3. Введите IP шлюза: 192.168.1.1
# 4. Введите IP жертв: 192.168.1.100 192.168.1.101
```

## 🔒 Меры безопасности

### Защита от ARP-spoofing атак
```bash
# Статические ARP записи
sudo arp -s <шлюз> <MAC шлюза>

# Использование ARPwatch
sudo apt install arpwatch
sudo systemctl start arpwatch

# Настройка IDS/IPS систем
```

### Обнаружение атак
- **ARPwatch** - мониторинг ARP трафика
- **Wireshark** - анализ сетевого трафика
- **Snort** - система обнаружения вторжений


## ⚠️ Отказ от ответственности

Автор не несет ответственности за любое незаконное использование этого программного обеспечения. Этот инструмент предназначен только для образовательных целей и тестирования безопасности собственных сетей.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request
