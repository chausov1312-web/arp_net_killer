ARP Internet Killer Tool 🔥

Мощный инструмент для демонстрации уязвимости ARP-протокола в локальных сетях. Программа выполняет ARP-spoofing атаку, позволяя временно лишить выбранное устройство доступа к интернету.

⚠️ ВНИМАНИЕ: ПРЕДУПРЕЖДЕНИЕ ОБ ИСПОЛЬЗОВАНИИ

ЭТО ИНСТРУМЕНТ ДЛЯ ТЕСТИРОВАНИЯ ЗАЩИТЫ И ОБРАЗОВАТЕЛЬНЫХ ЦЕЛЕЙ!

· Используйте ТОЛЬКО в собственной лабораторной среде или с явного разрешения владельца сети
· Неиспользование в соответствии с законом может привести к уголовной ответственности
· Этот инструмент демонстрирует уязвимость, чтобы помочь защитить сети

📋 Основные возможности

· 🔍 Агрессивное сканирование сети - обнаружение всех активных устройств
· 🎯 Автоматическое определение - IP, MAC, шлюза, маски сети
· 📡 Несколько методов сканирования - nmap + ARP пинг
· 🖥️ Интуитивный интерфейс - поддержка fzf для удобного выбора
· 🔄 Автоматическое восстановление - восстановление ARP-таблиц после атаки
· ⚡ Мультипоточная атака - эффективная отправка ARP-пакетов

📦 Зависимости

Установите необходимые пакеты:

Для Debian/Ubuntu:

```bash
sudo apt update
sudo apt install python3 python3-pip nmap fzf
sudo pip3 install scapy
```

Для Arch Linux:

```bash
sudo pacman -S python python-pip nmap fzf
sudo pip install scapy
```

🚀 Установка и запуск

1. Скачайте скрипт:

```bash
wget https://raw.githubusercontent.com/ваш-репозиторий/arp_killer.py -O arp_killer.py
```

1. Дайте права на выполнение:

```bash
chmod +x arp_killer.py
```

1. Запустите с правами root:

```bash
sudo ./arp_killer.py
```

Или напрямую через Python:

```bash
sudo python3 arp_killer.py
```

🎮 Как использовать

Шаг 1: Выбор интерфейса

Программа автоматически определит доступные сетевые интерфейсы:

· eth0 - Ethernet соединение
· wlan0 - Wi-Fi соединение
· usb0 - USB-модемы

Шаг 2: Автоматическое определение

Скрипт автоматически определит:

· Ваш IP и MAC адрес
· Маску подсети
· Шлюз по умолчанию (роутер)

Шаг 3: Режим работы

Выберите один из режимов:

🔍 Режим сканирования (рекомендуется)

· Автоматически найдет все устройства в сети
· Покажет список с IP и MAC адресами
· Позволит выбрать жертву и шлюз из списка

📝 Ручной режим

· Ввод всех параметров вручную
· Полезно, если автоматическое сканирование не работает

Шаг 4: Подтверждение атаки

Перед запуском вы увидите сводку:

```
🌐 Шлюз:    192.168.1.1 (aa:bb:cc:dd:ee:ff)
🎯 Жертва:  192.168.1.100 (11:22:33:44:55:66)
📡 Интерфейс: eth0
```

Шаг 5: Запуск атаки

· Атака начнется после подтверждения
· Статус будет отображаться в реальном времени
· Нажмите Ctrl+C для остановки

Шаг 6: Автоматическое восстановление

При остановке (Ctrl+C) программа:

· Отправит корректные ARP-пакеты
· Восстановит ARP-таблицы жертвы
· Выведет статистику атаки

🔧 Технические детали

Методы сканирования сети

1. NMAP (если установлен) - наиболее надежный метод
2. ARP Ping - агрессивный ARP-запрос ко всем адресам
3. Комбинированный подход - для максимального покрытия

Механизм атаки

1. Отправка поддельных ARP-ответов жертве
2. Подмена MAC-адреса шлюза
3. Перенаправление трафика жертвы
4. Использование случайных MAC-адресов для усложнения обнаружения

Восстановление

1. Отправка корректных ARP-пакетов
2. 20 итераций для гарантированного восстановления
3. Задержка 0.1с между пакетами

🛡️ Меры защиты от подобных атак

Для администраторов сетей:

```bash
# Статическая ARP-таблица
arp -s IP_адрес MAC_адрес

# Мониторинг ARP-трафика
arpwatch -i eth0

# Использование DHCP snooping на коммутаторах
```

Рекомендуемые инструменты защиты:

· ARPwatch - мониторинг ARP-таблиц
· Snort - система обнаружения вторжений
· XArp - графический мониторинг ARP-трафика
· Статическая ARP - фиксация ARP-записей для критических устройств

⚠️ Ограничения и известные проблемы

Не работает в следующих случаях:

· В сетях с защитой от ARP-spoofing
· При использовании VPN
· В сегментированных сетях (VLAN)
· С современными ОС с защитой от ARP-атак

Требования:

· Обязательно: права root/administrator
· Желательно: физический доступ к сети
· Рекомендуется: тестовая лабораторная среда

📚 Образовательные цели

Этот инструмент полезен для:

1. Изучения сетевой безопасности
2. Тестирования защитных механизмов
3. Демонстрации уязвимостей ARP
4. Обучения методам защиты сетей

🤝 Вклад в развитие

Мы приветствуем:

· Сообщения об ошибках
· Предложения по улучшению
· Pull requests

Пожалуйста, не используйте этот инструмент во вред!

📄 Лицензия

Этот инструмент предоставляется только для образовательных целей. Автор не несет ответственности за неправомерное использование.

---

Запомните: Сетевые атаки без разрешения являются преступлением во многих странах. Используйте знания для защиты, а не для атак.

Stay ethical, stay legal! 🔐